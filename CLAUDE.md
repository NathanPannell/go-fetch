When planning, create a concise plan in the form of a todo list markdown file in the root of the repo.

When making changes, before finishing, create tests for any new features/changes and verify that everything works with `make test-integration`. You can also do manual testing with `curl` and `docker compose` to test the API.